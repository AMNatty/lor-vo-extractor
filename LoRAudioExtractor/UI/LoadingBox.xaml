<UserControl x:Class="LoRAudioExtractor.UI.LoadingBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoRAudioExtractor.UI"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600">
    <Grid Width="Auto" Height="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Content="Loading..." Margin="5" FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Bottom" />
        <ProgressBar HorizontalAlignment="Left" Height="10" Margin="10" Grid.Row="1" VerticalAlignment="Top" Width="500" IsIndeterminate="True"/>

    </Grid>
</UserControl>

<UserControl x:Class="LoRAudioExtractor.UI.AppInitialBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoRAudioExtractor.UI"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border x:Name="ChildContainer" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label 
            Content="Please begin by opening a game directory."
            FontSize="24"
            Opacity="0.2" />
    </Border>
</UserControl>
